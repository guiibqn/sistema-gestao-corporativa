version: '3.8'

services:
  db:
    image: postgres:15
    container_name: gestao_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: gestao_corporativa
    ports:
      - "5432:5432"